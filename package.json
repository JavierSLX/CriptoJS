{
  "name": "CriptoNodeJS",
  "version": "1.0.0",
  "description": "ssh-keygen -o\r ssh-keygen -f public.key -e -m PKCS8 > public.key.pem",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
